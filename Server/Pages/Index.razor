@page "/"
@inject IApplicationConfig AppConfig
@inject IDataService DataService
 
<AuthorizeView>
    <NotAuthorized>
        <div class="jumbotron">
            <h3 class="display-4">Le garage à PC</h3>
            <p class="lead">
                Solution de téléassistance.
            </p>
            <hr class="my-4">
            <p class="lead">
                <a class="btn btn-primary btn-lg mr-2" href="Identity/Account/Login" role="button">Connexion</a>

                @if (AppConfig.MaxOrganizationCount < 0 || DataService.GetOrganizationCount() < AppConfig.MaxOrganizationCount)
                {
                    <a class="btn btn-primary btn-lg" href="Identity/Account/Register" role="button">Inscription</a>
                }
            </p>
        </div>
    </NotAuthorized>
    <Authorized>
        <AuthorizedIndex />
    </Authorized>
</AuthorizeView>
